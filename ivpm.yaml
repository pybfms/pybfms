package:
    name: pybfms

    dep-sets:
    - name: default
      deps:
      - name: cocotb
        type: python
        src: pypi

    - name: default-dev
      deps:
      - name: cocotb
        type: python
        src: pypi

    paths:
      export:
        lib-dirs:
          - verilog/rtl
      project:
        lib-dirs:
          - verilog/rtl
